
  # Food Ordering App

  This is a code bundle for Food Ordering App. The original project is available at https://www.figma.com/design/9pdOdEV82tZX0PKL6IQcDr/Food-Ordering-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  